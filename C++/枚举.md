

一个数值（都是整数）的集合

```cpp
#include <iostream>
// 冒号后面是指定数据类型，只能是整数（默认为 int）
enum Example : unsigned char 
{
	// 默认从 0 开始递增，可以认为设定
	A = 10,B,C
};

int main()
{
	Example value = B;
	
	if(value == 11)
	{
		// 会执行，B == 11
	}
	
	return 0;
}
```