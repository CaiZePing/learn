```cpp
#include <iostream>

class Entity
{
public:
	Entity()
	{
		std::cout << "Create Entity!" << std::endl;
	}
	~Entity()
	{
		std::cout << "Destroyed Entity!" << std::endl;
	}
};

class ScopedPtr
{
private:
	Enityt*m_Ptr;
public:
	ScopedPtr(Entity*ptr)
		:m_Ptr(ptr){}
	~ScopedPtr()
	{
		delete m_ptr;
	}
};

int main()
{
	{
		Entity e1;
		Entity* eptr = new Entity();
		SocpedPtr entity = new Entity();
	}
	// 在上面作用域结束的时候
	// e1 已经销毁了
	// eptr 没有被释放
	// 只能等程序结束，被操作系统销毁
	// entity 也将 new 出来的 Entity 销毁了
	
	return 0;
	
}
```